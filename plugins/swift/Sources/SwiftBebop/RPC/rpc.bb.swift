// Code generated by bebopc-gen-swift. DO NOT EDIT.
// source: /Users/andrew/projects/bebop-vnext/bebop/schemas/bebop/rpc.bop
// bebopc 2026.0.0-alpha.3
// edition 2026
//
// This file requires the Bebop runtime library.
// https://github.com/6over3/bebop
//
// The Bebop compiler and runtime are licensed under the Apache License,
// Version 2.0. You may obtain a copy of the License at:
// https://www.apache.org/licenses/LICENSE-2.0
//
// SPDX-License-Identifier: Apache-2.0
// Copyright (c) 6OVER3 INSTITUTE
// swiftlint:disable all

// @@bebop_insertion_point(imports)

/// Codes 0-16 align with gRPC. 17-255 are application-defined.
public struct StatusCode: RawRepresentable, Sendable, Hashable, BebopRecord, BebopReflectable {
    public typealias View = StatusCode

    public let rawValue: UInt8

    public init(rawValue: UInt8) { self.rawValue = rawValue }

    public static let ok = StatusCode(rawValue: 0)

    public static let cancelled = StatusCode(rawValue: 1)

    public static let unknown = StatusCode(rawValue: 2)

    public static let invalidArgument = StatusCode(rawValue: 3)

    public static let deadlineExceeded = StatusCode(rawValue: 4)

    public static let notFound = StatusCode(rawValue: 5)

    public static let permissionDenied = StatusCode(rawValue: 7)

    public static let resourceExhausted = StatusCode(rawValue: 8)

    public static let unimplemented = StatusCode(rawValue: 12)

    public static let `internal` = StatusCode(rawValue: 13)

    public static let unavailable = StatusCode(rawValue: 14)

    public static let unauthenticated = StatusCode(rawValue: 16)

    public static func decode(from reader: inout BebopReader) throws -> StatusCode {
        return StatusCode(rawValue: try reader.readByte())
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        StatusCode(rawValue: try reader.readByte())
    }

    public func encode(to writer: inout BebopWriter) {
        writer.writeByte(rawValue)
    }

    public var encodedSize: Int { 1 }

    public static let bebopReflection = BebopTypeReflection(
        name: "StatusCode",
        fqn: "bebop.StatusCode",
        kind: .enum,
        detail: .enum(
            EnumReflection(
                members: [
                    (name: "OK", value: 0),
                    (name: "CANCELLED", value: 1),
                    (name: "UNKNOWN", value: 2),
                    (name: "INVALID_ARGUMENT", value: 3),
                    (name: "DEADLINE_EXCEEDED", value: 4),
                    (name: "NOT_FOUND", value: 5),
                    (name: "PERMISSION_DENIED", value: 7),
                    (name: "RESOURCE_EXHAUSTED", value: 8),
                    (name: "UNIMPLEMENTED", value: 12),
                    (name: "INTERNAL", value: 13),
                    (name: "UNAVAILABLE", value: 14),
                    (name: "UNAUTHENTICATED", value: 16)
                ],
                isFlags: false
            )
        )
    )

    // @@bebop_insertion_point(enum_scope:StatusCode)
}

public struct FrameFlags: OptionSet, Sendable, BebopRecord, BebopReflectable {
    public typealias View = FrameFlags

    public let rawValue: UInt8

    public init(rawValue: UInt8) { self.rawValue = rawValue }

    /// Last frame in this direction.
    public static let endStream = FrameFlags(rawValue: 1)

    /// Payload is an RpcError.
    public static let error = FrameFlags(rawValue: 2)

    /// Payload is compressed. Algorithm negotiated via metadata.
    public static let compressed = FrameFlags(rawValue: 4)

    /// Payload is a TrailingMetadata.
    public static let trailer = FrameFlags(rawValue: 8)

    /// 8-byte LE uint64 cursor follows the payload.
    public static let cursor = FrameFlags(rawValue: 16)

    public static func decode(from reader: inout BebopReader) throws -> FrameFlags {
        return FrameFlags(rawValue: try reader.readByte())
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        FrameFlags(rawValue: try reader.readByte())
    }

    public func encode(to writer: inout BebopWriter) {
        writer.writeByte(rawValue)
    }

    public var encodedSize: Int { 1 }

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        self.rawValue = try container.decode(UInt8.self)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(rawValue)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "FrameFlags",
        fqn: "bebop.FrameFlags",
        kind: .enum,
        detail: .enum(
            EnumReflection(
                members: [
                    (name: "NONE", value: 0),
                    (name: "END_STREAM", value: 1),
                    (name: "ERROR", value: 2),
                    (name: "COMPRESSED", value: 4),
                    (name: "TRAILER", value: 8),
                    (name: "CURSOR", value: 16)
                ],
                isFlags: true
            )
        )
    )

    // @@bebop_insertion_point(enum_scope:FrameFlags)
}

public struct MethodType: RawRepresentable, Sendable, Hashable, BebopRecord, BebopReflectable {
    public typealias View = MethodType

    public let rawValue: UInt8

    public init(rawValue: UInt8) { self.rawValue = rawValue }

    public static let unary = MethodType(rawValue: 0)

    public static let serverStream = MethodType(rawValue: 1)

    public static let clientStream = MethodType(rawValue: 2)

    public static let duplexStream = MethodType(rawValue: 3)

    public static func decode(from reader: inout BebopReader) throws -> MethodType {
        return MethodType(rawValue: try reader.readByte())
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        MethodType(rawValue: try reader.readByte())
    }

    public func encode(to writer: inout BebopWriter) {
        writer.writeByte(rawValue)
    }

    public var encodedSize: Int { 1 }

    public static let bebopReflection = BebopTypeReflection(
        name: "MethodType",
        fqn: "bebop.MethodType",
        kind: .enum,
        detail: .enum(
            EnumReflection(
                members: [
                    (name: "UNARY", value: 0),
                    (name: "SERVER_STREAM", value: 1),
                    (name: "CLIENT_STREAM", value: 2),
                    (name: "DUPLEX_STREAM", value: 3)
                ],
                isFlags: false
            )
        )
    )

    // @@bebop_insertion_point(enum_scope:MethodType)
}

/// 9 bytes, fixed layout, no tags. Precedes every payload on the wire.
public struct FrameHeader: BebopRecord, BebopReflectable {
    /// Payload byte count.
    public let length: UInt32

    public let flags: FrameFlags

    /// 0 when the transport already isolates calls (HTTP/2, separate connections).
    public let streamId: UInt32

    enum CodingKeys: String, CodingKey {
        case length
        case flags
        case streamId = "stream_id"
    }

    public init(length: UInt32, flags: FrameFlags, streamId: UInt32) {
        self.length = length
        self.flags = flags
        self.streamId = streamId
    }

    public static func decode(from reader: inout BebopReader) throws -> FrameHeader {
        // @@bebop_insertion_point(decode_start:FrameHeader)
        let length = try reader.readUInt32()
        let flags = try reader.readNested { _nestedReader0 in try FrameFlags.decode(from: &_nestedReader0) }
        let streamId = try reader.readUInt32()
        // @@bebop_insertion_point(decode_end:FrameHeader)
        return FrameHeader(length: length, flags: flags, streamId: streamId)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:FrameHeader)
        writer.writeUInt32(length)
        flags.encode(to: &writer)
        writer.writeUInt32(streamId)
        // @@bebop_insertion_point(encode_end:FrameHeader)
    }

    public var encodedSize: Int {
        var size = 0
        size += 4
        size += flags.encodedSize
        size += 4
        return size
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        /// Payload byte count.
        public let length: UInt32

        public let flags: FrameFlags.View

        /// 0 when the transport already isolates calls (HTTP/2, separate connections).
        public let streamId: UInt32

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try FrameHeader.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, length: UInt32, flags: FrameFlags.View, streamId: UInt32) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.length = length
            self.flags = flags
            self.streamId = streamId
        }

        public func decoded() throws -> FrameHeader {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try FrameHeader.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let length = try reader.readUInt32()
        let flags = try reader.readNested { _nestedReader0 in try FrameFlags.readView(from: &_nestedReader0) }
        let streamId = try reader.readUInt32()
        return View(validated: reader.view(from: start), limits: reader.limits, length: length, flags: flags, streamId: streamId)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "FrameHeader",
        fqn: "bebop.FrameHeader",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "length",
                    index: 0,
                    typeName: "UInt32"
                ),
                BebopFieldReflection(
                    name: "flags",
                    index: 0,
                    typeName: "FrameFlags"
                ),
                BebopFieldReflection(
                    name: "stream_id",
                    index: 0,
                    typeName: "UInt32"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:FrameHeader)
}

/// Sent once at the start of a binary transport call. On HTTP, these
/// fields map to URL path, headers, and the bebop-deadline header.
public struct CallHeader: BebopRecord, BebopReflectable {
    private final class Storage: @unchecked Sendable {
        var methodId: UInt32?
        var deadline: BebopTimestamp?
        var metadata: [String: String]?
        var cursor: UInt64?
        init(methodId: UInt32? = nil, deadline: BebopTimestamp? = nil, metadata: [String: String]? = nil, cursor: UInt64? = nil) {
            self.methodId = methodId
            self.deadline = deadline
            self.metadata = metadata
            self.cursor = cursor
        }
        func copy() -> Storage { Storage(methodId: methodId, deadline: deadline, metadata: metadata, cursor: cursor) }
    }

    private var storage: Storage

    /// MurmurHash3 of /ServiceName/MethodName.
    public var methodId: UInt32? {
        get { storage.methodId }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.methodId = newValue
        }
    }

    /// Absolute timestamp. Omit for no deadline.
    public var deadline: BebopTimestamp? {
        get { storage.deadline }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.deadline = newValue
        }
    }

    /// Keys: ASCII lowercase, digits, hyphens, underscores.
    /// Keys starting with "bebop-" are reserved.
    public var metadata: [String: String]? {
        get { storage.metadata }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.metadata = newValue
        }
    }

    /// Stream resume position. 0 = start from beginning.
    public var cursor: UInt64? {
        get { storage.cursor }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.cursor = newValue
        }
    }

    public init(methodId: UInt32? = nil, deadline: BebopTimestamp? = nil, metadata: [String: String]? = nil, cursor: UInt64? = nil) { storage = Storage(methodId: methodId, deadline: deadline, metadata: metadata, cursor: cursor) }

    public static func == (lhs: CallHeader, rhs: CallHeader) -> Bool {
        if lhs.methodId != rhs.methodId { return false }
        if lhs.deadline != rhs.deadline { return false }
        if lhs.metadata != rhs.metadata { return false }
        if lhs.cursor != rhs.cursor { return false }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(methodId)
        hasher.combine(deadline)
        hasher.combine(metadata)
        hasher.combine(cursor)
    }

    public static func decode(from reader: inout BebopReader) throws -> CallHeader {
        return try reader.readMessage { withField in
            // @@bebop_insertion_point(decode_start:CallHeader)
            var methodId: UInt32? = nil
            var deadline: BebopTimestamp? = nil
            var metadata: [String: String]? = nil
            var cursor: UInt64? = nil
            try withField(1) { fieldReader in
                methodId = try fieldReader.readUInt32()
            }
            try withField(2) { fieldReader in
                deadline = try fieldReader.readTimestamp()
            }
            try withField(3) { fieldReader in
                metadata = try fieldReader.readDynamicMap { _entryReader0 in
                    let key = try _entryReader0.readString()
                    let value = try _entryReader0.readString()
                    return (key, value)
                }
            }
            try withField(4) { fieldReader in
                cursor = try fieldReader.readUInt64()
            }
            // @@bebop_insertion_point(decode_end:CallHeader)
            return CallHeader(methodId: methodId, deadline: deadline, metadata: metadata, cursor: cursor)
        }
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:CallHeader)
        let payloadStart = writer.beginMessage()
        var tags = InlineArray<4, UInt8> { _ in 0 }
        var offsets = InlineArray<4, UInt32> { _ in 0 }
        var fieldCount = 0
        if let _v = methodId {
            tags[fieldCount] = 1
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeUInt32(_v)
        }
        if let _v = deadline {
            tags[fieldCount] = 2
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeTimestamp(_v)
        }
        if let _v = metadata {
            tags[fieldCount] = 3
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeDynamicMap(_v) { _entryWriter0, _key0, _value0 in
                _entryWriter0.writeString(_key0)
                _entryWriter0.writeString(_value0)
            }
        }
        if let _v = cursor {
            tags[fieldCount] = 4
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeUInt64(_v)
        }
        writer.endMessage(payloadStart: payloadStart, tags: tags, offsets: offsets, count: fieldCount)
        // @@bebop_insertion_point(encode_end:CallHeader)
    }

    public var encodedSize: Int {
        var tags = InlineArray<4, UInt8> { _ in 0 }
        var fieldCount = 0
        var payloadSize = 0
        if methodId != nil { tags[fieldCount] = 1; fieldCount += 1; payloadSize += 4 }
        if deadline != nil { tags[fieldCount] = 2; fieldCount += 1; payloadSize += 16 }
        if let _v = metadata { tags[fieldCount] = 3; fieldCount += 1; payloadSize += (4 + _v.reduce(0) { _acc, _kv in let (_k, _v) = _kv; return _acc + (4 + _k.utf8.count + 1) + (4 + _v.utf8.count + 1) }) }
        if cursor != nil { tags[fieldCount] = 4; fieldCount += 1; payloadSize += 8 }
        return BebopMessageLayout.encodedSize(payloadSize: payloadSize, tags: tags, count: fieldCount)
    }

    enum CodingKeys: String, CodingKey {
        case methodId = "method_id"
        case deadline
        case metadata
        case cursor
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(methodId, forKey: .methodId)
        try container.encodeIfPresent(deadline, forKey: .deadline)
        try container.encodeIfPresent(metadata, forKey: .metadata)
        try container.encodeIfPresent(cursor, forKey: .cursor)
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.init()
        methodId = try container.decodeIfPresent(UInt32.self, forKey: .methodId)
        deadline = try container.decodeIfPresent(BebopTimestamp.self, forKey: .deadline)
        metadata = try container.decodeIfPresent([String: String].self, forKey: .metadata)
        cursor = try container.decodeIfPresent(UInt64.self, forKey: .cursor)
    }

    public struct View: BebopRecordView {
        private let message: BebopMessageView

        /// MurmurHash3 of /ServiceName/MethodName.
        public var methodId: UInt32? {
            get throws {
                try message.decodeField(1) { fieldReader in
                    try fieldReader.readUInt32()
                }
            }
        }

        /// Absolute timestamp. Omit for no deadline.
        public var deadline: BebopTimestamp? {
            get throws {
                try message.decodeField(2) { fieldReader in
                    try fieldReader.readTimestamp()
                }
            }
        }

        /// Keys: ASCII lowercase, digits, hyphens, underscores.
        /// Keys starting with "bebop-" are reserved.
        public var metadata: BebopMapView<BebopStringView, BebopStringView>? {
            get throws {
                try message.decodeField(3) { fieldReader in
                    try fieldReader.readMapView(
                        key: { _keyReader0 in
                            try _keyReader0.readStringView()
                        },
                        value: { _valueReader0 in
                            try _valueReader0.readStringView()
                        }
                    )
                }
            }
        }

        /// Stream resume position. 0 = start from beginning.
        public var cursor: UInt64? {
            get throws {
                try message.decodeField(4) { fieldReader in
                    try fieldReader.readUInt64()
                }
            }
        }

        public var encoded: BebopView { message.encoded }

        public func field(_ tag: UInt8) -> BebopView? { message.field(tag) }

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws { try self.init(indexed: BebopMessageView(encoded, limits: limits)) }

        fileprivate init(indexed message: BebopMessageView) {
            self.message = message
        }

        public func decoded() throws -> CallHeader {
            try message.encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: message.limits)
                return try CallHeader.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        try View(indexed: reader.readMessageView())
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "CallHeader",
        fqn: "bebop.CallHeader",
        kind: .message,
        detail: .message(
            MessageReflection(fields: [
                BebopFieldReflection(
                    name: "method_id",
                    index: 1,
                    typeName: "UInt32"
                ),
                BebopFieldReflection(
                    name: "deadline",
                    index: 2,
                    typeName: "BebopTimestamp"
                ),
                BebopFieldReflection(
                    name: "metadata",
                    index: 3,
                    typeName: "[String: String]"
                ),
                BebopFieldReflection(
                    name: "cursor",
                    index: 4,
                    typeName: "UInt64"
                )
            ])
        )
    )

    // @@bebop_insertion_point(message_scope:CallHeader)
}

/// Payload of an ERROR frame. On error, response metadata goes here
/// instead of in a separate TrailingMetadata frame.
public struct RpcError: BebopRecord, BebopReflectable {
    private final class Storage: @unchecked Sendable {
        var code: StatusCode?
        var detail: String?
        var metadata: [String: String]?
        init(code: StatusCode? = nil, detail: String? = nil, metadata: [String: String]? = nil) {
            self.code = code
            self.detail = detail
            self.metadata = metadata
        }
        func copy() -> Storage { Storage(code: code, detail: detail, metadata: metadata) }
    }

    private var storage: Storage

    public var code: StatusCode? {
        get { storage.code }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.code = newValue
        }
    }

    public var detail: String? {
        get { storage.detail }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.detail = newValue
        }
    }

    public var metadata: [String: String]? {
        get { storage.metadata }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.metadata = newValue
        }
    }

    public init(code: StatusCode? = nil, detail: String? = nil, metadata: [String: String]? = nil) { storage = Storage(code: code, detail: detail, metadata: metadata) }

    public static func == (lhs: RpcError, rhs: RpcError) -> Bool {
        if lhs.code != rhs.code { return false }
        if lhs.detail != rhs.detail { return false }
        if lhs.metadata != rhs.metadata { return false }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(code)
        hasher.combine(detail)
        hasher.combine(metadata)
    }

    public static func decode(from reader: inout BebopReader) throws -> RpcError {
        return try reader.readMessage { withField in
            // @@bebop_insertion_point(decode_start:RpcError)
            var code: StatusCode? = nil
            var detail: String? = nil
            var metadata: [String: String]? = nil
            try withField(1) { fieldReader in
                code = try fieldReader.readNested { _nestedReader0 in try StatusCode.decode(from: &_nestedReader0) }
            }
            try withField(2) { fieldReader in
                detail = try fieldReader.readString()
            }
            try withField(3) { fieldReader in
                metadata = try fieldReader.readDynamicMap { _entryReader0 in
                    let key = try _entryReader0.readString()
                    let value = try _entryReader0.readString()
                    return (key, value)
                }
            }
            // @@bebop_insertion_point(decode_end:RpcError)
            return RpcError(code: code, detail: detail, metadata: metadata)
        }
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:RpcError)
        let payloadStart = writer.beginMessage()
        var tags = InlineArray<3, UInt8> { _ in 0 }
        var offsets = InlineArray<3, UInt32> { _ in 0 }
        var fieldCount = 0
        if let _v = code {
            tags[fieldCount] = 1
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            _v.encode(to: &writer)
        }
        if let _v = detail {
            tags[fieldCount] = 2
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeString(_v)
        }
        if let _v = metadata {
            tags[fieldCount] = 3
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeDynamicMap(_v) { _entryWriter0, _key0, _value0 in
                _entryWriter0.writeString(_key0)
                _entryWriter0.writeString(_value0)
            }
        }
        writer.endMessage(payloadStart: payloadStart, tags: tags, offsets: offsets, count: fieldCount)
        // @@bebop_insertion_point(encode_end:RpcError)
    }

    public var encodedSize: Int {
        var tags = InlineArray<3, UInt8> { _ in 0 }
        var fieldCount = 0
        var payloadSize = 0
        if let _v = code { tags[fieldCount] = 1; fieldCount += 1; payloadSize += _v.encodedSize }
        if let _v = detail { tags[fieldCount] = 2; fieldCount += 1; payloadSize += (4 + _v.utf8.count + 1) }
        if let _v = metadata { tags[fieldCount] = 3; fieldCount += 1; payloadSize += (4 + _v.reduce(0) { _acc, _kv in let (_k, _v) = _kv; return _acc + (4 + _k.utf8.count + 1) + (4 + _v.utf8.count + 1) }) }
        return BebopMessageLayout.encodedSize(payloadSize: payloadSize, tags: tags, count: fieldCount)
    }

    enum CodingKeys: String, CodingKey {
        case code
        case detail
        case metadata
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(code, forKey: .code)
        try container.encodeIfPresent(detail, forKey: .detail)
        try container.encodeIfPresent(metadata, forKey: .metadata)
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.init()
        code = try container.decodeIfPresent(StatusCode.self, forKey: .code)
        detail = try container.decodeIfPresent(String.self, forKey: .detail)
        metadata = try container.decodeIfPresent([String: String].self, forKey: .metadata)
    }

    public struct View: BebopRecordView {
        private let message: BebopMessageView

        public var code: StatusCode.View? {
            get throws {
                try message.decodeField(1) { fieldReader in
                    try fieldReader.readNested { _nestedReader0 in try StatusCode.readView(from: &_nestedReader0) }
                }
            }
        }

        public var detail: BebopStringView? {
            get throws {
                try message.decodeField(2) { fieldReader in
                    try fieldReader.readStringView()
                }
            }
        }

        public var metadata: BebopMapView<BebopStringView, BebopStringView>? {
            get throws {
                try message.decodeField(3) { fieldReader in
                    try fieldReader.readMapView(
                        key: { _keyReader0 in
                            try _keyReader0.readStringView()
                        },
                        value: { _valueReader0 in
                            try _valueReader0.readStringView()
                        }
                    )
                }
            }
        }

        public var encoded: BebopView { message.encoded }

        public func field(_ tag: UInt8) -> BebopView? { message.field(tag) }

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws { try self.init(indexed: BebopMessageView(encoded, limits: limits)) }

        fileprivate init(indexed message: BebopMessageView) {
            self.message = message
        }

        public func decoded() throws -> RpcError {
            try message.encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: message.limits)
                return try RpcError.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        try View(indexed: reader.readMessageView())
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "RpcError",
        fqn: "bebop.RpcError",
        kind: .message,
        detail: .message(
            MessageReflection(fields: [
                BebopFieldReflection(
                    name: "code",
                    index: 1,
                    typeName: "StatusCode"
                ),
                BebopFieldReflection(
                    name: "detail",
                    index: 2,
                    typeName: "String"
                ),
                BebopFieldReflection(
                    name: "metadata",
                    index: 3,
                    typeName: "[String: String]"
                )
            ])
        )
    )

    // @@bebop_insertion_point(message_scope:RpcError)
}

/// Payload of a TRAILER frame. Sent after the last response data frame.
public struct TrailingMetadata: BebopRecord, BebopReflectable {
    private final class Storage: @unchecked Sendable {
        var metadata: [String: String]?
        init(metadata: [String: String]? = nil) {
            self.metadata = metadata
        }
        func copy() -> Storage { Storage(metadata: metadata) }
    }

    private var storage: Storage

    public var metadata: [String: String]? {
        get { storage.metadata }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.metadata = newValue
        }
    }

    public init(metadata: [String: String]? = nil) { storage = Storage(metadata: metadata) }

    public static func == (lhs: TrailingMetadata, rhs: TrailingMetadata) -> Bool {
        if lhs.metadata != rhs.metadata { return false }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(metadata)
    }

    public static func decode(from reader: inout BebopReader) throws -> TrailingMetadata {
        return try reader.readMessage { withField in
            // @@bebop_insertion_point(decode_start:TrailingMetadata)
            var metadata: [String: String]? = nil
            try withField(1) { fieldReader in
                metadata = try fieldReader.readDynamicMap { _entryReader0 in
                    let key = try _entryReader0.readString()
                    let value = try _entryReader0.readString()
                    return (key, value)
                }
            }
            // @@bebop_insertion_point(decode_end:TrailingMetadata)
            return TrailingMetadata(metadata: metadata)
        }
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:TrailingMetadata)
        let payloadStart = writer.beginMessage()
        var tags = InlineArray<1, UInt8> { _ in 0 }
        var offsets = InlineArray<1, UInt32> { _ in 0 }
        var fieldCount = 0
        if let _v = metadata {
            tags[fieldCount] = 1
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeDynamicMap(_v) { _entryWriter0, _key0, _value0 in
                _entryWriter0.writeString(_key0)
                _entryWriter0.writeString(_value0)
            }
        }
        writer.endMessage(payloadStart: payloadStart, tags: tags, offsets: offsets, count: fieldCount)
        // @@bebop_insertion_point(encode_end:TrailingMetadata)
    }

    public var encodedSize: Int {
        var tags = InlineArray<1, UInt8> { _ in 0 }
        var fieldCount = 0
        var payloadSize = 0
        if let _v = metadata { tags[fieldCount] = 1; fieldCount += 1; payloadSize += (4 + _v.reduce(0) { _acc, _kv in let (_k, _v) = _kv; return _acc + (4 + _k.utf8.count + 1) + (4 + _v.utf8.count + 1) }) }
        return BebopMessageLayout.encodedSize(payloadSize: payloadSize, tags: tags, count: fieldCount)
    }

    enum CodingKeys: String, CodingKey {
        case metadata
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(metadata, forKey: .metadata)
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.init()
        metadata = try container.decodeIfPresent([String: String].self, forKey: .metadata)
    }

    public struct View: BebopRecordView {
        private let message: BebopMessageView

        public var metadata: BebopMapView<BebopStringView, BebopStringView>? {
            get throws {
                try message.decodeField(1) { fieldReader in
                    try fieldReader.readMapView(
                        key: { _keyReader0 in
                            try _keyReader0.readStringView()
                        },
                        value: { _valueReader0 in
                            try _valueReader0.readStringView()
                        }
                    )
                }
            }
        }

        public var encoded: BebopView { message.encoded }

        public func field(_ tag: UInt8) -> BebopView? { message.field(tag) }

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws { try self.init(indexed: BebopMessageView(encoded, limits: limits)) }

        fileprivate init(indexed message: BebopMessageView) {
            self.message = message
        }

        public func decoded() throws -> TrailingMetadata {
            try message.encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: message.limits)
                return try TrailingMetadata.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        try View(indexed: reader.readMessageView())
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "TrailingMetadata",
        fqn: "bebop.TrailingMetadata",
        kind: .message,
        detail: .message(
            MessageReflection(fields: [
                BebopFieldReflection(
                    name: "metadata",
                    index: 1,
                    typeName: "[String: String]"
                )
            ])
        )
    )

    // @@bebop_insertion_point(message_scope:TrailingMetadata)
}

public struct MethodInfo: BebopRecord, BebopReflectable {
    public let name: String

    public let methodId: UInt32

    public let methodType: MethodType

    /// e.g. "type.bebop.sh/myapp.HelloRequest"
    public let requestTypeUrl: String

    /// e.g. "type.bebop.sh/myapp.HelloResponse"
    public let responseTypeUrl: String

    enum CodingKeys: String, CodingKey {
        case name
        case methodId = "method_id"
        case methodType = "method_type"
        case requestTypeUrl = "request_type_url"
        case responseTypeUrl = "response_type_url"
    }

    public init(name: String, methodId: UInt32, methodType: MethodType, requestTypeUrl: String, responseTypeUrl: String) {
        self.name = name
        self.methodId = methodId
        self.methodType = methodType
        self.requestTypeUrl = requestTypeUrl
        self.responseTypeUrl = responseTypeUrl
    }

    public static func decode(from reader: inout BebopReader) throws -> MethodInfo {
        // @@bebop_insertion_point(decode_start:MethodInfo)
        let name = try reader.readString()
        let methodId = try reader.readUInt32()
        let methodType = try reader.readNested { _nestedReader0 in try MethodType.decode(from: &_nestedReader0) }
        let requestTypeUrl = try reader.readString()
        let responseTypeUrl = try reader.readString()
        // @@bebop_insertion_point(decode_end:MethodInfo)
        return MethodInfo(name: name, methodId: methodId, methodType: methodType, requestTypeUrl: requestTypeUrl, responseTypeUrl: responseTypeUrl)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:MethodInfo)
        writer.writeString(name)
        writer.writeUInt32(methodId)
        methodType.encode(to: &writer)
        writer.writeString(requestTypeUrl)
        writer.writeString(responseTypeUrl)
        // @@bebop_insertion_point(encode_end:MethodInfo)
    }

    public var encodedSize: Int {
        var size = 0
        size += (4 + name.utf8.count + 1)
        size += 4
        size += methodType.encodedSize
        size += (4 + requestTypeUrl.utf8.count + 1)
        size += (4 + responseTypeUrl.utf8.count + 1)
        return size
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        public let name: BebopStringView

        public let methodId: UInt32

        public let methodType: MethodType.View

        /// e.g. "type.bebop.sh/myapp.HelloRequest"
        public let requestTypeUrl: BebopStringView

        /// e.g. "type.bebop.sh/myapp.HelloResponse"
        public let responseTypeUrl: BebopStringView

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try MethodInfo.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, name: BebopStringView, methodId: UInt32, methodType: MethodType.View, requestTypeUrl: BebopStringView, responseTypeUrl: BebopStringView) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.name = name
            self.methodId = methodId
            self.methodType = methodType
            self.requestTypeUrl = requestTypeUrl
            self.responseTypeUrl = responseTypeUrl
        }

        public func decoded() throws -> MethodInfo {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try MethodInfo.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let name = try reader.readStringView()
        let methodId = try reader.readUInt32()
        let methodType = try reader.readNested { _nestedReader0 in try MethodType.readView(from: &_nestedReader0) }
        let requestTypeUrl = try reader.readStringView()
        let responseTypeUrl = try reader.readStringView()
        return View(validated: reader.view(from: start), limits: reader.limits, name: name, methodId: methodId, methodType: methodType, requestTypeUrl: requestTypeUrl, responseTypeUrl: responseTypeUrl)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "MethodInfo",
        fqn: "bebop.MethodInfo",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "name",
                    index: 0,
                    typeName: "String"
                ),
                BebopFieldReflection(
                    name: "method_id",
                    index: 0,
                    typeName: "UInt32"
                ),
                BebopFieldReflection(
                    name: "method_type",
                    index: 0,
                    typeName: "MethodType"
                ),
                BebopFieldReflection(
                    name: "request_type_url",
                    index: 0,
                    typeName: "String"
                ),
                BebopFieldReflection(
                    name: "response_type_url",
                    index: 0,
                    typeName: "String"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:MethodInfo)
}

public struct ServiceInfo: BebopRecord, BebopReflectable {
    public let name: String

    public let methods: [MethodInfo]

    enum CodingKeys: String, CodingKey {
        case name
        case methods
    }

    public init(name: String, methods: [MethodInfo]) {
        self.name = name
        self.methods = methods
    }

    public static func decode(from reader: inout BebopReader) throws -> ServiceInfo {
        // @@bebop_insertion_point(decode_start:ServiceInfo)
        let name = try reader.readString()
        let methods = try reader.readDynamicArray { _elementReader0 in
            try _elementReader0.readNested { _nestedReader1 in try MethodInfo.decode(from: &_nestedReader1) }
        }
        // @@bebop_insertion_point(decode_end:ServiceInfo)
        return ServiceInfo(name: name, methods: methods)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:ServiceInfo)
        writer.writeString(name)
        writer.writeDynamicArray(methods) { _elementWriter0, _element0 in
            _element0.encode(to: &_elementWriter0)
        }
        // @@bebop_insertion_point(encode_end:ServiceInfo)
    }

    public var encodedSize: Int {
        var size = 0
        size += (4 + name.utf8.count + 1)
        size += (4 + methods.reduce(0) { _acc, _el in _acc + _el.encodedSize })
        return size
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        public let name: BebopStringView

        public let methods: BebopArrayView<MethodInfo.View>

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try ServiceInfo.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, name: BebopStringView, methods: BebopArrayView<MethodInfo.View>) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.name = name
            self.methods = methods
        }

        public func decoded() throws -> ServiceInfo {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try ServiceInfo.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let name = try reader.readStringView()
        let methods = try reader.readArrayView { _elementReader0 in
            try _elementReader0.readNested { _nestedReader1 in try MethodInfo.readView(from: &_nestedReader1) }
        }
        return View(validated: reader.view(from: start), limits: reader.limits, name: name, methods: methods)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "ServiceInfo",
        fqn: "bebop.ServiceInfo",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "name",
                    index: 0,
                    typeName: "String"
                ),
                BebopFieldReflection(
                    name: "methods",
                    index: 0,
                    typeName: "[MethodInfo]"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:ServiceInfo)
}

/// Method ID 0. Request: bebop.Empty. Servers that don't support
/// discovery return UNIMPLEMENTED.
public struct DiscoveryResponse: BebopRecord, BebopReflectable {
    public let services: [ServiceInfo]

    enum CodingKeys: String, CodingKey {
        case services
    }

    public init(services: [ServiceInfo]) {
        self.services = services
    }

    public static func decode(from reader: inout BebopReader) throws -> DiscoveryResponse {
        // @@bebop_insertion_point(decode_start:DiscoveryResponse)
        let services = try reader.readDynamicArray { _elementReader0 in
            try _elementReader0.readNested { _nestedReader1 in try ServiceInfo.decode(from: &_nestedReader1) }
        }
        // @@bebop_insertion_point(decode_end:DiscoveryResponse)
        return DiscoveryResponse(services: services)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:DiscoveryResponse)
        writer.writeDynamicArray(services) { _elementWriter0, _element0 in
            _element0.encode(to: &_elementWriter0)
        }
        // @@bebop_insertion_point(encode_end:DiscoveryResponse)
    }

    public var encodedSize: Int {
        var size = 0
        size += (4 + services.reduce(0) { _acc, _el in _acc + _el.encodedSize })
        return size
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        public let services: BebopArrayView<ServiceInfo.View>

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try DiscoveryResponse.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, services: BebopArrayView<ServiceInfo.View>) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.services = services
        }

        public func decoded() throws -> DiscoveryResponse {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try DiscoveryResponse.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let services = try reader.readArrayView { _elementReader0 in
            try _elementReader0.readNested { _nestedReader1 in try ServiceInfo.readView(from: &_nestedReader1) }
        }
        return View(validated: reader.view(from: start), limits: reader.limits, services: services)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "DiscoveryResponse",
        fqn: "bebop.DiscoveryResponse",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "services",
                    index: 0,
                    typeName: "[ServiceInfo]"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:DiscoveryResponse)
}

/// When input_from >= 0, the server pipes the first payload from the
/// referenced call's result as this call's request bytes (payload field
/// is ignored). When input_from is -1, payload is used directly.
public struct BatchCall: BebopRecord, BebopReflectable {
    /// Zero-indexed, unique within the batch.
    public let callId: Int32

    /// MurmurHash3 of /ServiceName/MethodName.
    public let methodId: UInt32

    /// Wire-encoded request. Ignored when input_from >= 0.
    public let payload: [UInt8]

    /// Forward result of this call_id as input. -1 = use payload.
    public let inputFrom: Int32

    enum CodingKeys: String, CodingKey {
        case callId = "call_id"
        case methodId = "method_id"
        case payload
        case inputFrom = "input_from"
    }

    public init(callId: Int32, methodId: UInt32, payload: [UInt8], inputFrom: Int32) {
        self.callId = callId
        self.methodId = methodId
        self.payload = payload
        self.inputFrom = inputFrom
    }

    public static func decode(from reader: inout BebopReader) throws -> BatchCall {
        // @@bebop_insertion_point(decode_start:BatchCall)
        let callId = try reader.readInt32()
        let methodId = try reader.readUInt32()
        let payload = try reader.readLengthPrefixedArray(of: UInt8.self)
        let inputFrom = try reader.readInt32()
        // @@bebop_insertion_point(decode_end:BatchCall)
        return BatchCall(callId: callId, methodId: methodId, payload: payload, inputFrom: inputFrom)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:BatchCall)
        writer.writeInt32(callId)
        writer.writeUInt32(methodId)
        writer.writeLengthPrefixedArray(payload)
        writer.writeInt32(inputFrom)
        // @@bebop_insertion_point(encode_end:BatchCall)
    }

    public var encodedSize: Int {
        var size = 0
        size += 4
        size += 4
        size += (4 + payload.count * 1)
        size += 4
        return size
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        /// Zero-indexed, unique within the batch.
        public let callId: Int32

        /// MurmurHash3 of /ServiceName/MethodName.
        public let methodId: UInt32

        /// Wire-encoded request. Ignored when input_from >= 0.
        public let payload: BebopArrayView<UInt8>

        /// Forward result of this call_id as input. -1 = use payload.
        public let inputFrom: Int32

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try BatchCall.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, callId: Int32, methodId: UInt32, payload: BebopArrayView<UInt8>, inputFrom: Int32) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.callId = callId
            self.methodId = methodId
            self.payload = payload
            self.inputFrom = inputFrom
        }

        public func decoded() throws -> BatchCall {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try BatchCall.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let callId = try reader.readInt32()
        let methodId = try reader.readUInt32()
        let payload = try reader.readContiguousArrayView(elementSize: 1) { _elementReader0 in
            try _elementReader0.readByte()
        }
        let inputFrom = try reader.readInt32()
        return View(validated: reader.view(from: start), limits: reader.limits, callId: callId, methodId: methodId, payload: payload, inputFrom: inputFrom)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "BatchCall",
        fqn: "bebop.BatchCall",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "call_id",
                    index: 0,
                    typeName: "Int32"
                ),
                BebopFieldReflection(
                    name: "method_id",
                    index: 0,
                    typeName: "UInt32"
                ),
                BebopFieldReflection(
                    name: "payload",
                    index: 0,
                    typeName: "[UInt8]"
                ),
                BebopFieldReflection(
                    name: "input_from",
                    index: 0,
                    typeName: "Int32"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:BatchCall)
}

/// Method ID 1. The server parallelizes independent calls and resolves
/// input_from dependencies between layers. Only unary and server-stream
/// methods are supported; client-stream and duplex return INVALID_ARGUMENT.
public struct BatchRequest: BebopRecord, BebopReflectable {
    public let calls: [BatchCall]

    /// Applied to every call in the batch.
    public let metadata: [String: String]

    enum CodingKeys: String, CodingKey {
        case calls
        case metadata
    }

    public init(calls: [BatchCall], metadata: [String: String] = [:]) {
        self.calls = calls
        self.metadata = metadata
    }

    public static func decode(from reader: inout BebopReader) throws -> BatchRequest {
        // @@bebop_insertion_point(decode_start:BatchRequest)
        let calls = try reader.readDynamicArray { _elementReader0 in
            try _elementReader0.readNested { _nestedReader1 in try BatchCall.decode(from: &_nestedReader1) }
        }
        let metadata = try reader.readDynamicMap { _entryReader0 in
            let key = try _entryReader0.readString()
            let value = try _entryReader0.readString()
            return (key, value)
        }
        // @@bebop_insertion_point(decode_end:BatchRequest)
        return BatchRequest(calls: calls, metadata: metadata)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:BatchRequest)
        writer.writeDynamicArray(calls) { _elementWriter0, _element0 in
            _element0.encode(to: &_elementWriter0)
        }
        writer.writeDynamicMap(metadata) { _entryWriter0, _key0, _value0 in
            _entryWriter0.writeString(_key0)
            _entryWriter0.writeString(_value0)
        }
        // @@bebop_insertion_point(encode_end:BatchRequest)
    }

    public var encodedSize: Int {
        var size = 0
        size += (4 + calls.reduce(0) { _acc, _el in _acc + _el.encodedSize })
        size += (4 + metadata.reduce(0) { _acc, _kv in let (_k, _v) = _kv; return _acc + (4 + _k.utf8.count + 1) + (4 + _v.utf8.count + 1) })
        return size
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        public let calls: BebopArrayView<BatchCall.View>

        /// Applied to every call in the batch.
        public let metadata: BebopMapView<BebopStringView, BebopStringView>

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try BatchRequest.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, calls: BebopArrayView<BatchCall.View>, metadata: BebopMapView<BebopStringView, BebopStringView>) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.calls = calls
            self.metadata = metadata
        }

        public func decoded() throws -> BatchRequest {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try BatchRequest.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let calls = try reader.readArrayView { _elementReader0 in
            try _elementReader0.readNested { _nestedReader1 in try BatchCall.readView(from: &_nestedReader1) }
        }
        let metadata = try reader.readMapView(
            key: { _keyReader0 in
                try _keyReader0.readStringView()
            },
            value: { _valueReader0 in
                try _valueReader0.readStringView()
            }
        )
        return View(validated: reader.view(from: start), limits: reader.limits, calls: calls, metadata: metadata)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "BatchRequest",
        fqn: "bebop.BatchRequest",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "calls",
                    index: 0,
                    typeName: "[BatchCall]"
                ),
                BebopFieldReflection(
                    name: "metadata",
                    index: 0,
                    typeName: "[String: String]"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:BatchRequest)
}

/// One element per unary response. All buffered elements for server-stream.
public struct BatchSuccess: BebopRecord, BebopReflectable {
    public let payloads: [[UInt8]]

    public let metadata: [String: String]

    enum CodingKeys: String, CodingKey {
        case payloads
        case metadata
    }

    public init(payloads: [[UInt8]], metadata: [String: String] = [:]) {
        self.payloads = payloads
        self.metadata = metadata
    }

    public static func decode(from reader: inout BebopReader) throws -> BatchSuccess {
        // @@bebop_insertion_point(decode_start:BatchSuccess)
        let payloads = try reader.readDynamicArray { _elementReader0 in
            try _elementReader0.readLengthPrefixedArray(of: UInt8.self)
        }
        let metadata = try reader.readDynamicMap { _entryReader0 in
            let key = try _entryReader0.readString()
            let value = try _entryReader0.readString()
            return (key, value)
        }
        // @@bebop_insertion_point(decode_end:BatchSuccess)
        return BatchSuccess(payloads: payloads, metadata: metadata)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:BatchSuccess)
        writer.writeDynamicArray(payloads) { _elementWriter0, _element0 in
            _elementWriter0.writeLengthPrefixedArray(_element0)
        }
        writer.writeDynamicMap(metadata) { _entryWriter0, _key0, _value0 in
            _entryWriter0.writeString(_key0)
            _entryWriter0.writeString(_value0)
        }
        // @@bebop_insertion_point(encode_end:BatchSuccess)
    }

    public var encodedSize: Int {
        var size = 0
        size += (4 + payloads.reduce(0) { _acc, _el in _acc + (4 + _el.count * 1) })
        size += (4 + metadata.reduce(0) { _acc, _kv in let (_k, _v) = _kv; return _acc + (4 + _k.utf8.count + 1) + (4 + _v.utf8.count + 1) })
        return size
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        public let payloads: BebopArrayView<BebopArrayView<UInt8>>

        public let metadata: BebopMapView<BebopStringView, BebopStringView>

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try BatchSuccess.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, payloads: BebopArrayView<BebopArrayView<UInt8>>, metadata: BebopMapView<BebopStringView, BebopStringView>) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.payloads = payloads
            self.metadata = metadata
        }

        public func decoded() throws -> BatchSuccess {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try BatchSuccess.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let payloads = try reader.readArrayView { _elementReader0 in
            try _elementReader0.readContiguousArrayView(elementSize: 1) { _elementReader1 in
                try _elementReader1.readByte()
            }
        }
        let metadata = try reader.readMapView(
            key: { _keyReader0 in
                try _keyReader0.readStringView()
            },
            value: { _valueReader0 in
                try _valueReader0.readStringView()
            }
        )
        return View(validated: reader.view(from: start), limits: reader.limits, payloads: payloads, metadata: metadata)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "BatchSuccess",
        fqn: "bebop.BatchSuccess",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "payloads",
                    index: 0,
                    typeName: "[[UInt8]]"
                ),
                BebopFieldReflection(
                    name: "metadata",
                    index: 0,
                    typeName: "[String: String]"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:BatchSuccess)
}

public enum BatchOutcome: BebopRecord, BebopReflectable, Codable {
    case unknown(discriminator: UInt8, data: [UInt8])

    case success(BatchSuccess)

    case error(RpcError)

    public static func decode(from reader: inout BebopReader) throws -> BatchOutcome {
        // @@bebop_insertion_point(decode_start:BatchOutcome)
        let length = try reader.beginLengthPrefixedValue()
        let end = reader.position + Int(length)
        let disc = try reader.readByte()
        let result: BatchOutcome
        switch disc {
        case 1:
            result = .success(try reader.readNested { nested in try BatchSuccess.decode(from: &nested) })
        case 2:
            result = .error(try reader.readNested { nested in try RpcError.decode(from: &nested) })
        // @@bebop_insertion_point(decode_switch:BatchOutcome)
        default:
            let remaining = end - reader.position
            let data = try reader.readBytes(remaining)
            result = .unknown(discriminator: disc, data: data)
        }
        guard reader.position == end else {
            throw BebopDecodingError.trailingData
        }
        // @@bebop_insertion_point(decode_end:BatchOutcome)
        return result
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:BatchOutcome)
        let pos = writer.beginLengthPrefixedValue()
        switch self {
        case .success(let _v):
            writer.writeByte(1)
            _v.encode(to: &writer)
        case .error(let _v):
            writer.writeByte(2)
            _v.encode(to: &writer)
        // @@bebop_insertion_point(encode_switch:BatchOutcome)
        case .unknown(let disc, let data):
            writer.writeByte(disc)
            writer.writeBytes(data)
        }
        writer.endLengthPrefixedValue(at: pos)
        // @@bebop_insertion_point(encode_end:BatchOutcome)
    }

    public var encodedSize: Int {
        switch self {
        case .success(let _v):
            return 4 + 1 + _v.encodedSize
        case .error(let _v):
            return 4 + 1 + _v.encodedSize
        case .unknown(_, let data):
            return 4 + 1 + data.count
        }
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        public enum Content: Sendable {
            case unknown(discriminator: UInt8, data: BebopView)
            case success(BatchSuccess.View)
            case error(RpcError.View)
        }

        public let discriminator: UInt8

        public let value: Content

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try BatchOutcome.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, discriminator: UInt8, value: Content) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.discriminator = discriminator
            self.value = value
        }

        public func decoded() throws -> BatchOutcome {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try BatchOutcome.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let result = try reader.readLengthPrefixedValue { body in
            let discriminator = try body.readByte()
            let value: View.Content
            switch discriminator {
            case 1: value = .success(try body.readNested { nested in try BatchSuccess.readView(from: &nested) })
            case 2: value = .error(try body.readNested { nested in try RpcError.readView(from: &nested) })
            default: value = .unknown(discriminator: discriminator, data: try body.readBytesView(count: body.remaining))
            }
            return (discriminator, value)
        }
        return View(validated: result.encoded, limits: reader.limits, discriminator: result.value.0, value: result.value.1)
    }

    enum CodingKeys: String, CodingKey { case discriminator, value }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        let disc = try container.decode(UInt8.self, forKey: .discriminator)
        switch disc {
        case 1:
            self = .success(try container.decode(BatchSuccess.self, forKey: .value))
        case 2:
            self = .error(try container.decode(RpcError.self, forKey: .value))
        default:
            let data = try container.decode([UInt8].self, forKey: .value)
            self = .unknown(discriminator: disc, data: data)
        }
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        switch self {
        case .success(let _v):
            try container.encode(UInt8(1), forKey: .discriminator)
            try container.encode(_v, forKey: .value)
        case .error(let _v):
            try container.encode(UInt8(2), forKey: .discriminator)
            try container.encode(_v, forKey: .value)
        case .unknown(let disc, let data):
            try container.encode(disc, forKey: .discriminator)
            try container.encode(data, forKey: .value)
        }
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "BatchOutcome",
        fqn: "bebop.BatchOutcome",
        kind: .union,
        detail: .union(
            UnionReflection(branches: [
                (discriminator: 1, name: "success", typeName: "BatchSuccess"),
                (discriminator: 2, name: "error", typeName: "RpcError")
            ])
        )
    )

    // @@bebop_insertion_point(union_scope:BatchOutcome)
}

/// Results in the same order as calls in the request.
public struct BatchResult: BebopRecord, BebopReflectable {
    public let callId: Int32

    public let outcome: BatchOutcome

    enum CodingKeys: String, CodingKey {
        case callId = "call_id"
        case outcome
    }

    public init(callId: Int32, outcome: BatchOutcome) {
        self.callId = callId
        self.outcome = outcome
    }

    public static func decode(from reader: inout BebopReader) throws -> BatchResult {
        // @@bebop_insertion_point(decode_start:BatchResult)
        let callId = try reader.readInt32()
        let outcome = try reader.readNested { _nestedReader0 in try BatchOutcome.decode(from: &_nestedReader0) }
        // @@bebop_insertion_point(decode_end:BatchResult)
        return BatchResult(callId: callId, outcome: outcome)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:BatchResult)
        writer.writeInt32(callId)
        outcome.encode(to: &writer)
        // @@bebop_insertion_point(encode_end:BatchResult)
    }

    public var encodedSize: Int {
        var size = 0
        size += 4
        size += outcome.encodedSize
        return size
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        public let callId: Int32

        public let outcome: BatchOutcome.View

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try BatchResult.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, callId: Int32, outcome: BatchOutcome.View) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.callId = callId
            self.outcome = outcome
        }

        public func decoded() throws -> BatchResult {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try BatchResult.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let callId = try reader.readInt32()
        let outcome = try reader.readNested { _nestedReader0 in try BatchOutcome.readView(from: &_nestedReader0) }
        return View(validated: reader.view(from: start), limits: reader.limits, callId: callId, outcome: outcome)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "BatchResult",
        fqn: "bebop.BatchResult",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "call_id",
                    index: 0,
                    typeName: "Int32"
                ),
                BebopFieldReflection(
                    name: "outcome",
                    index: 0,
                    typeName: "BatchOutcome"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:BatchResult)
}

/// If a call fails, dependent calls (via input_from) also fail.
public struct BatchResponse: BebopRecord, BebopReflectable {
    public let results: [BatchResult]

    enum CodingKeys: String, CodingKey {
        case results
    }

    public init(results: [BatchResult]) {
        self.results = results
    }

    public static func decode(from reader: inout BebopReader) throws -> BatchResponse {
        // @@bebop_insertion_point(decode_start:BatchResponse)
        let results = try reader.readDynamicArray { _elementReader0 in
            try _elementReader0.readNested { _nestedReader1 in try BatchResult.decode(from: &_nestedReader1) }
        }
        // @@bebop_insertion_point(decode_end:BatchResponse)
        return BatchResponse(results: results)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:BatchResponse)
        writer.writeDynamicArray(results) { _elementWriter0, _element0 in
            _element0.encode(to: &_elementWriter0)
        }
        // @@bebop_insertion_point(encode_end:BatchResponse)
    }

    public var encodedSize: Int {
        var size = 0
        size += (4 + results.reduce(0) { _acc, _el in _acc + _el.encodedSize })
        return size
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        public let results: BebopArrayView<BatchResult.View>

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try BatchResponse.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, results: BebopArrayView<BatchResult.View>) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.results = results
        }

        public func decoded() throws -> BatchResponse {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try BatchResponse.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let results = try reader.readArrayView { _elementReader0 in
            try _elementReader0.readNested { _nestedReader1 in try BatchResult.readView(from: &_nestedReader1) }
        }
        return View(validated: reader.view(from: start), limits: reader.limits, results: results)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "BatchResponse",
        fqn: "bebop.BatchResponse",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "results",
                    index: 0,
                    typeName: "[BatchResult]"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:BatchResponse)
}

/// Method ID 2 request. Wraps any unary or batch call for async execution.
/// The server runs the inner call in the background and returns a handle
/// immediately. Use FutureResolveRequest (method 3) to receive results.
public struct FutureDispatchRequest: BebopRecord, BebopReflectable {
    private final class Storage: @unchecked Sendable {
        var methodId: UInt32?
        var payload: [UInt8]?
        var idempotencyKey: BebopUUID?
        var metadata: [String: String]?
        var deadline: BebopTimestamp?
        var discardResult: Bool?
        init(methodId: UInt32? = nil, payload: [UInt8]? = nil, idempotencyKey: BebopUUID? = nil, metadata: [String: String]? = nil, deadline: BebopTimestamp? = nil, discardResult: Bool? = nil) {
            self.methodId = methodId
            self.payload = payload
            self.idempotencyKey = idempotencyKey
            self.metadata = metadata
            self.deadline = deadline
            self.discardResult = discardResult
        }
        func copy() -> Storage { Storage(methodId: methodId, payload: payload, idempotencyKey: idempotencyKey, metadata: metadata, deadline: deadline, discardResult: discardResult) }
    }

    private var storage: Storage

    /// MurmurHash3 of /ServiceName/MethodName for the inner call.
    public var methodId: UInt32? {
        get { storage.methodId }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.methodId = newValue
        }
    }

    /// Wire-encoded request for the inner call.
    public var payload: [UInt8]? {
        get { storage.payload }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.payload = newValue
        }
    }

    /// Client-assigned dedup key. If a pending or completed future with the
    /// same key exists, the server returns its handle instead of dispatching
    /// again. Omit for no deduplication.
    public var idempotencyKey: BebopUUID? {
        get { storage.idempotencyKey }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.idempotencyKey = newValue
        }
    }

    /// Metadata forwarded to the inner call's RpcContext.
    public var metadata: [String: String]? {
        get { storage.metadata }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.metadata = newValue
        }
    }

    /// Absolute deadline for the inner call. Omit for no deadline.
    public var deadline: BebopTimestamp? {
        get { storage.deadline }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.deadline = newValue
        }
    }

    /// When true, the server discards the result after delivering it to
    /// active resolve streams. Rehydration is unavailable. Omit or false
    /// for default retention.
    public var discardResult: Bool? {
        get { storage.discardResult }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.discardResult = newValue
        }
    }

    public init(methodId: UInt32? = nil, payload: [UInt8]? = nil, idempotencyKey: BebopUUID? = nil, metadata: [String: String]? = nil, deadline: BebopTimestamp? = nil, discardResult: Bool? = nil) { storage = Storage(methodId: methodId, payload: payload, idempotencyKey: idempotencyKey, metadata: metadata, deadline: deadline, discardResult: discardResult) }

    public static func == (lhs: FutureDispatchRequest, rhs: FutureDispatchRequest) -> Bool {
        if lhs.methodId != rhs.methodId { return false }
        if lhs.payload != rhs.payload { return false }
        if lhs.idempotencyKey != rhs.idempotencyKey { return false }
        if lhs.metadata != rhs.metadata { return false }
        if lhs.deadline != rhs.deadline { return false }
        if lhs.discardResult != rhs.discardResult { return false }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(methodId)
        hasher.combine(payload)
        hasher.combine(idempotencyKey)
        hasher.combine(metadata)
        hasher.combine(deadline)
        hasher.combine(discardResult)
    }

    public static func decode(from reader: inout BebopReader) throws -> FutureDispatchRequest {
        return try reader.readMessage { withField in
            // @@bebop_insertion_point(decode_start:FutureDispatchRequest)
            var methodId: UInt32? = nil
            var payload: [UInt8]? = nil
            var idempotencyKey: BebopUUID? = nil
            var metadata: [String: String]? = nil
            var deadline: BebopTimestamp? = nil
            var discardResult: Bool? = nil
            try withField(1) { fieldReader in
                methodId = try fieldReader.readUInt32()
            }
            try withField(2) { fieldReader in
                payload = try fieldReader.readLengthPrefixedArray(of: UInt8.self)
            }
            try withField(3) { fieldReader in
                idempotencyKey = try fieldReader.readUUID()
            }
            try withField(4) { fieldReader in
                metadata = try fieldReader.readDynamicMap { _entryReader0 in
                    let key = try _entryReader0.readString()
                    let value = try _entryReader0.readString()
                    return (key, value)
                }
            }
            try withField(5) { fieldReader in
                deadline = try fieldReader.readTimestamp()
            }
            try withField(6) { fieldReader in
                discardResult = try fieldReader.readBool()
            }
            // @@bebop_insertion_point(decode_end:FutureDispatchRequest)
            return FutureDispatchRequest(methodId: methodId, payload: payload, idempotencyKey: idempotencyKey, metadata: metadata, deadline: deadline, discardResult: discardResult)
        }
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:FutureDispatchRequest)
        let payloadStart = writer.beginMessage()
        var tags = InlineArray<6, UInt8> { _ in 0 }
        var offsets = InlineArray<6, UInt32> { _ in 0 }
        var fieldCount = 0
        if let _v = methodId {
            tags[fieldCount] = 1
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeUInt32(_v)
        }
        if let _v = payload {
            tags[fieldCount] = 2
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeLengthPrefixedArray(_v)
        }
        if let _v = idempotencyKey {
            tags[fieldCount] = 3
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeUUID(_v)
        }
        if let _v = metadata {
            tags[fieldCount] = 4
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeDynamicMap(_v) { _entryWriter0, _key0, _value0 in
                _entryWriter0.writeString(_key0)
                _entryWriter0.writeString(_value0)
            }
        }
        if let _v = deadline {
            tags[fieldCount] = 5
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeTimestamp(_v)
        }
        if let _v = discardResult {
            tags[fieldCount] = 6
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeBool(_v)
        }
        writer.endMessage(payloadStart: payloadStart, tags: tags, offsets: offsets, count: fieldCount)
        // @@bebop_insertion_point(encode_end:FutureDispatchRequest)
    }

    public var encodedSize: Int {
        var tags = InlineArray<6, UInt8> { _ in 0 }
        var fieldCount = 0
        var payloadSize = 0
        if methodId != nil { tags[fieldCount] = 1; fieldCount += 1; payloadSize += 4 }
        if let _v = payload { tags[fieldCount] = 2; fieldCount += 1; payloadSize += (4 + _v.count * 1) }
        if idempotencyKey != nil { tags[fieldCount] = 3; fieldCount += 1; payloadSize += 16 }
        if let _v = metadata { tags[fieldCount] = 4; fieldCount += 1; payloadSize += (4 + _v.reduce(0) { _acc, _kv in let (_k, _v) = _kv; return _acc + (4 + _k.utf8.count + 1) + (4 + _v.utf8.count + 1) }) }
        if deadline != nil { tags[fieldCount] = 5; fieldCount += 1; payloadSize += 16 }
        if discardResult != nil { tags[fieldCount] = 6; fieldCount += 1; payloadSize += 1 }
        return BebopMessageLayout.encodedSize(payloadSize: payloadSize, tags: tags, count: fieldCount)
    }

    enum CodingKeys: String, CodingKey {
        case methodId = "method_id"
        case payload
        case idempotencyKey = "idempotency_key"
        case metadata
        case deadline
        case discardResult = "discard_result"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(methodId, forKey: .methodId)
        try container.encodeIfPresent(payload, forKey: .payload)
        try container.encodeIfPresent(idempotencyKey, forKey: .idempotencyKey)
        try container.encodeIfPresent(metadata, forKey: .metadata)
        try container.encodeIfPresent(deadline, forKey: .deadline)
        try container.encodeIfPresent(discardResult, forKey: .discardResult)
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.init()
        methodId = try container.decodeIfPresent(UInt32.self, forKey: .methodId)
        payload = try container.decodeIfPresent([UInt8].self, forKey: .payload)
        idempotencyKey = try container.decodeIfPresent(BebopUUID.self, forKey: .idempotencyKey)
        metadata = try container.decodeIfPresent([String: String].self, forKey: .metadata)
        deadline = try container.decodeIfPresent(BebopTimestamp.self, forKey: .deadline)
        discardResult = try container.decodeIfPresent(Bool.self, forKey: .discardResult)
    }

    public struct View: BebopRecordView {
        private let message: BebopMessageView

        /// MurmurHash3 of /ServiceName/MethodName for the inner call.
        public var methodId: UInt32? {
            get throws {
                try message.decodeField(1) { fieldReader in
                    try fieldReader.readUInt32()
                }
            }
        }

        /// Wire-encoded request for the inner call.
        public var payload: BebopArrayView<UInt8>? {
            get throws {
                try message.decodeField(2) { fieldReader in
                    try fieldReader.readContiguousArrayView(elementSize: 1) { _elementReader0 in
                        try _elementReader0.readByte()
                    }
                }
            }
        }

        /// Client-assigned dedup key. If a pending or completed future with the
        /// same key exists, the server returns its handle instead of dispatching
        /// again. Omit for no deduplication.
        public var idempotencyKey: BebopUUID? {
            get throws {
                try message.decodeField(3) { fieldReader in
                    try fieldReader.readUUID()
                }
            }
        }

        /// Metadata forwarded to the inner call's RpcContext.
        public var metadata: BebopMapView<BebopStringView, BebopStringView>? {
            get throws {
                try message.decodeField(4) { fieldReader in
                    try fieldReader.readMapView(
                        key: { _keyReader0 in
                            try _keyReader0.readStringView()
                        },
                        value: { _valueReader0 in
                            try _valueReader0.readStringView()
                        }
                    )
                }
            }
        }

        /// Absolute deadline for the inner call. Omit for no deadline.
        public var deadline: BebopTimestamp? {
            get throws {
                try message.decodeField(5) { fieldReader in
                    try fieldReader.readTimestamp()
                }
            }
        }

        /// When true, the server discards the result after delivering it to
        /// active resolve streams. Rehydration is unavailable. Omit or false
        /// for default retention.
        public var discardResult: Bool? {
            get throws {
                try message.decodeField(6) { fieldReader in
                    try fieldReader.readBool()
                }
            }
        }

        public var encoded: BebopView { message.encoded }

        public func field(_ tag: UInt8) -> BebopView? { message.field(tag) }

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws { try self.init(indexed: BebopMessageView(encoded, limits: limits)) }

        fileprivate init(indexed message: BebopMessageView) {
            self.message = message
        }

        public func decoded() throws -> FutureDispatchRequest {
            try message.encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: message.limits)
                return try FutureDispatchRequest.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        try View(indexed: reader.readMessageView())
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "FutureDispatchRequest",
        fqn: "bebop.FutureDispatchRequest",
        kind: .message,
        detail: .message(
            MessageReflection(fields: [
                BebopFieldReflection(
                    name: "method_id",
                    index: 1,
                    typeName: "UInt32"
                ),
                BebopFieldReflection(
                    name: "payload",
                    index: 2,
                    typeName: "[UInt8]"
                ),
                BebopFieldReflection(
                    name: "idempotency_key",
                    index: 3,
                    typeName: "BebopUUID"
                ),
                BebopFieldReflection(
                    name: "metadata",
                    index: 4,
                    typeName: "[String: String]"
                ),
                BebopFieldReflection(
                    name: "deadline",
                    index: 5,
                    typeName: "BebopTimestamp"
                ),
                BebopFieldReflection(
                    name: "discard_result",
                    index: 6,
                    typeName: "Bool"
                )
            ])
        )
    )

    // @@bebop_insertion_point(message_scope:FutureDispatchRequest)
}

/// Method ID 2 response. Returned immediately after dispatch.
public struct FutureHandle: BebopRecord, BebopReflectable {
    /// Server-generated, unguessable identifier for the dispatched future.
    public let id: BebopUUID

    enum CodingKeys: String, CodingKey {
        case id
    }

    public init(id: BebopUUID) {
        self.id = id
    }

    public static func decode(from reader: inout BebopReader) throws -> FutureHandle {
        // @@bebop_insertion_point(decode_start:FutureHandle)
        let id = try reader.readUUID()
        // @@bebop_insertion_point(decode_end:FutureHandle)
        return FutureHandle(id: id)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:FutureHandle)
        writer.writeUUID(id)
        // @@bebop_insertion_point(encode_end:FutureHandle)
    }

    public var encodedSize: Int { 16 }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        /// Server-generated, unguessable identifier for the dispatched future.
        public let id: BebopUUID

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try FutureHandle.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, id: BebopUUID) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.id = id
        }

        public func decoded() throws -> FutureHandle {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try FutureHandle.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let id = try reader.readUUID()
        return View(validated: reader.view(from: start), limits: reader.limits, id: id)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "FutureHandle",
        fqn: "bebop.FutureHandle",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "id",
                    index: 0,
                    typeName: "BebopUUID"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:FutureHandle)
}

/// Method ID 3 request. Opens a server-stream that pushes FutureResult
/// as futures complete. On reconnection, pass IDs of still-pending futures;
/// the server pushes any already-completed results first, then continues
/// pushing as remaining futures finish.
public struct FutureResolveRequest: BebopRecord, BebopReflectable {
    private final class Storage: @unchecked Sendable {
        var ids: [BebopUUID]?
        init(ids: [BebopUUID]? = nil) {
            self.ids = ids
        }
        func copy() -> Storage { Storage(ids: ids) }
    }

    private var storage: Storage

    /// Future IDs to subscribe to. Omit to receive all futures for this session.
    public var ids: [BebopUUID]? {
        get { storage.ids }
        set {
            if !isKnownUniquelyReferenced(&storage) { storage = storage.copy() }
            storage.ids = newValue
        }
    }

    public init(ids: [BebopUUID]? = nil) { storage = Storage(ids: ids) }

    public static func == (lhs: FutureResolveRequest, rhs: FutureResolveRequest) -> Bool {
        if lhs.ids != rhs.ids { return false }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(ids)
    }

    public static func decode(from reader: inout BebopReader) throws -> FutureResolveRequest {
        return try reader.readMessage { withField in
            // @@bebop_insertion_point(decode_start:FutureResolveRequest)
            var ids: [BebopUUID]? = nil
            try withField(1) { fieldReader in
                ids = try fieldReader.readLengthPrefixedArray(of: BebopUUID.self)
            }
            // @@bebop_insertion_point(decode_end:FutureResolveRequest)
            return FutureResolveRequest(ids: ids)
        }
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:FutureResolveRequest)
        let payloadStart = writer.beginMessage()
        var tags = InlineArray<1, UInt8> { _ in 0 }
        var offsets = InlineArray<1, UInt32> { _ in 0 }
        var fieldCount = 0
        if let _v = ids {
            tags[fieldCount] = 1
            offsets[fieldCount] = UInt32(writer.position - payloadStart)
            fieldCount += 1
            writer.writeLengthPrefixedArray(_v)
        }
        writer.endMessage(payloadStart: payloadStart, tags: tags, offsets: offsets, count: fieldCount)
        // @@bebop_insertion_point(encode_end:FutureResolveRequest)
    }

    public var encodedSize: Int {
        var tags = InlineArray<1, UInt8> { _ in 0 }
        var fieldCount = 0
        var payloadSize = 0
        if let _v = ids { tags[fieldCount] = 1; fieldCount += 1; payloadSize += (4 + _v.count * 16) }
        return BebopMessageLayout.encodedSize(payloadSize: payloadSize, tags: tags, count: fieldCount)
    }

    enum CodingKeys: String, CodingKey {
        case ids
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(ids, forKey: .ids)
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.init()
        ids = try container.decodeIfPresent([BebopUUID].self, forKey: .ids)
    }

    public struct View: BebopRecordView {
        private let message: BebopMessageView

        /// Future IDs to subscribe to. Omit to receive all futures for this session.
        public var ids: BebopArrayView<BebopUUID>? {
            get throws {
                try message.decodeField(1) { fieldReader in
                    try fieldReader.readContiguousArrayView(elementSize: 16) { _elementReader0 in
                        try _elementReader0.readUUID()
                    }
                }
            }
        }

        public var encoded: BebopView { message.encoded }

        public func field(_ tag: UInt8) -> BebopView? { message.field(tag) }

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws { try self.init(indexed: BebopMessageView(encoded, limits: limits)) }

        fileprivate init(indexed message: BebopMessageView) {
            self.message = message
        }

        public func decoded() throws -> FutureResolveRequest {
            try message.encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: message.limits)
                return try FutureResolveRequest.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        try View(indexed: reader.readMessageView())
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "FutureResolveRequest",
        fqn: "bebop.FutureResolveRequest",
        kind: .message,
        detail: .message(
            MessageReflection(fields: [
                BebopFieldReflection(
                    name: "ids",
                    index: 1,
                    typeName: "[BebopUUID]"
                )
            ])
        )
    )

    // @@bebop_insertion_point(message_scope:FutureResolveRequest)
}

/// Payload of a successfully completed future.
public struct FutureSuccess: BebopRecord, BebopReflectable {
    /// Wire-encoded response from the inner call.
    public let payload: [UInt8]

    /// Response metadata set by the inner call's handler.
    public let metadata: [String: String]

    enum CodingKeys: String, CodingKey {
        case payload
        case metadata
    }

    public init(payload: [UInt8], metadata: [String: String] = [:]) {
        self.payload = payload
        self.metadata = metadata
    }

    public static func decode(from reader: inout BebopReader) throws -> FutureSuccess {
        // @@bebop_insertion_point(decode_start:FutureSuccess)
        let payload = try reader.readLengthPrefixedArray(of: UInt8.self)
        let metadata = try reader.readDynamicMap { _entryReader0 in
            let key = try _entryReader0.readString()
            let value = try _entryReader0.readString()
            return (key, value)
        }
        // @@bebop_insertion_point(decode_end:FutureSuccess)
        return FutureSuccess(payload: payload, metadata: metadata)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:FutureSuccess)
        writer.writeLengthPrefixedArray(payload)
        writer.writeDynamicMap(metadata) { _entryWriter0, _key0, _value0 in
            _entryWriter0.writeString(_key0)
            _entryWriter0.writeString(_value0)
        }
        // @@bebop_insertion_point(encode_end:FutureSuccess)
    }

    public var encodedSize: Int {
        var size = 0
        size += (4 + payload.count * 1)
        size += (4 + metadata.reduce(0) { _acc, _kv in let (_k, _v) = _kv; return _acc + (4 + _k.utf8.count + 1) + (4 + _v.utf8.count + 1) })
        return size
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        /// Wire-encoded response from the inner call.
        public let payload: BebopArrayView<UInt8>

        /// Response metadata set by the inner call's handler.
        public let metadata: BebopMapView<BebopStringView, BebopStringView>

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try FutureSuccess.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, payload: BebopArrayView<UInt8>, metadata: BebopMapView<BebopStringView, BebopStringView>) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.payload = payload
            self.metadata = metadata
        }

        public func decoded() throws -> FutureSuccess {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try FutureSuccess.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let payload = try reader.readContiguousArrayView(elementSize: 1) { _elementReader0 in
            try _elementReader0.readByte()
        }
        let metadata = try reader.readMapView(
            key: { _keyReader0 in
                try _keyReader0.readStringView()
            },
            value: { _valueReader0 in
                try _valueReader0.readStringView()
            }
        )
        return View(validated: reader.view(from: start), limits: reader.limits, payload: payload, metadata: metadata)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "FutureSuccess",
        fqn: "bebop.FutureSuccess",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "payload",
                    index: 0,
                    typeName: "[UInt8]"
                ),
                BebopFieldReflection(
                    name: "metadata",
                    index: 0,
                    typeName: "[String: String]"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:FutureSuccess)
}

/// Terminal state of a dispatched future.
public enum FutureOutcome: BebopRecord, BebopReflectable, Codable {
    case unknown(discriminator: UInt8, data: [UInt8])

    case success(FutureSuccess)

    case error(RpcError)

    public static func decode(from reader: inout BebopReader) throws -> FutureOutcome {
        // @@bebop_insertion_point(decode_start:FutureOutcome)
        let length = try reader.beginLengthPrefixedValue()
        let end = reader.position + Int(length)
        let disc = try reader.readByte()
        let result: FutureOutcome
        switch disc {
        case 1:
            result = .success(try reader.readNested { nested in try FutureSuccess.decode(from: &nested) })
        case 2:
            result = .error(try reader.readNested { nested in try RpcError.decode(from: &nested) })
        // @@bebop_insertion_point(decode_switch:FutureOutcome)
        default:
            let remaining = end - reader.position
            let data = try reader.readBytes(remaining)
            result = .unknown(discriminator: disc, data: data)
        }
        guard reader.position == end else {
            throw BebopDecodingError.trailingData
        }
        // @@bebop_insertion_point(decode_end:FutureOutcome)
        return result
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:FutureOutcome)
        let pos = writer.beginLengthPrefixedValue()
        switch self {
        case .success(let _v):
            writer.writeByte(1)
            _v.encode(to: &writer)
        case .error(let _v):
            writer.writeByte(2)
            _v.encode(to: &writer)
        // @@bebop_insertion_point(encode_switch:FutureOutcome)
        case .unknown(let disc, let data):
            writer.writeByte(disc)
            writer.writeBytes(data)
        }
        writer.endLengthPrefixedValue(at: pos)
        // @@bebop_insertion_point(encode_end:FutureOutcome)
    }

    public var encodedSize: Int {
        switch self {
        case .success(let _v):
            return 4 + 1 + _v.encodedSize
        case .error(let _v):
            return 4 + 1 + _v.encodedSize
        case .unknown(_, let data):
            return 4 + 1 + data.count
        }
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        public enum Content: Sendable {
            case unknown(discriminator: UInt8, data: BebopView)
            case success(FutureSuccess.View)
            case error(RpcError.View)
        }

        public let discriminator: UInt8

        public let value: Content

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try FutureOutcome.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, discriminator: UInt8, value: Content) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.discriminator = discriminator
            self.value = value
        }

        public func decoded() throws -> FutureOutcome {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try FutureOutcome.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let result = try reader.readLengthPrefixedValue { body in
            let discriminator = try body.readByte()
            let value: View.Content
            switch discriminator {
            case 1: value = .success(try body.readNested { nested in try FutureSuccess.readView(from: &nested) })
            case 2: value = .error(try body.readNested { nested in try RpcError.readView(from: &nested) })
            default: value = .unknown(discriminator: discriminator, data: try body.readBytesView(count: body.remaining))
            }
            return (discriminator, value)
        }
        return View(validated: result.encoded, limits: reader.limits, discriminator: result.value.0, value: result.value.1)
    }

    enum CodingKeys: String, CodingKey { case discriminator, value }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        let disc = try container.decode(UInt8.self, forKey: .discriminator)
        switch disc {
        case 1:
            self = .success(try container.decode(FutureSuccess.self, forKey: .value))
        case 2:
            self = .error(try container.decode(RpcError.self, forKey: .value))
        default:
            let data = try container.decode([UInt8].self, forKey: .value)
            self = .unknown(discriminator: disc, data: data)
        }
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        switch self {
        case .success(let _v):
            try container.encode(UInt8(1), forKey: .discriminator)
            try container.encode(_v, forKey: .value)
        case .error(let _v):
            try container.encode(UInt8(2), forKey: .discriminator)
            try container.encode(_v, forKey: .value)
        case .unknown(let disc, let data):
            try container.encode(disc, forKey: .discriminator)
            try container.encode(data, forKey: .value)
        }
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "FutureOutcome",
        fqn: "bebop.FutureOutcome",
        kind: .union,
        detail: .union(
            UnionReflection(branches: [
                (discriminator: 1, name: "success", typeName: "FutureSuccess"),
                (discriminator: 2, name: "error", typeName: "RpcError")
            ])
        )
    )

    // @@bebop_insertion_point(union_scope:FutureOutcome)
}

/// Pushed on the Resolve stream (method 3) as each future completes.
public struct FutureResult: BebopRecord, BebopReflectable {
    /// The future this result belongs to.
    public let id: BebopUUID

    /// Terminal outcome — success with payload or error.
    public let outcome: FutureOutcome

    enum CodingKeys: String, CodingKey {
        case id
        case outcome
    }

    public init(id: BebopUUID, outcome: FutureOutcome) {
        self.id = id
        self.outcome = outcome
    }

    public static func decode(from reader: inout BebopReader) throws -> FutureResult {
        // @@bebop_insertion_point(decode_start:FutureResult)
        let id = try reader.readUUID()
        let outcome = try reader.readNested { _nestedReader0 in try FutureOutcome.decode(from: &_nestedReader0) }
        // @@bebop_insertion_point(decode_end:FutureResult)
        return FutureResult(id: id, outcome: outcome)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:FutureResult)
        writer.writeUUID(id)
        outcome.encode(to: &writer)
        // @@bebop_insertion_point(encode_end:FutureResult)
    }

    public var encodedSize: Int {
        var size = 0
        size += 16
        size += outcome.encodedSize
        return size
    }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        /// The future this result belongs to.
        public let id: BebopUUID

        /// Terminal outcome — success with payload or error.
        public let outcome: FutureOutcome.View

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try FutureResult.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, id: BebopUUID, outcome: FutureOutcome.View) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.id = id
            self.outcome = outcome
        }

        public func decoded() throws -> FutureResult {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try FutureResult.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let id = try reader.readUUID()
        let outcome = try reader.readNested { _nestedReader0 in try FutureOutcome.readView(from: &_nestedReader0) }
        return View(validated: reader.view(from: start), limits: reader.limits, id: id, outcome: outcome)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "FutureResult",
        fqn: "bebop.FutureResult",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "id",
                    index: 0,
                    typeName: "BebopUUID"
                ),
                BebopFieldReflection(
                    name: "outcome",
                    index: 0,
                    typeName: "FutureOutcome"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:FutureResult)
}

/// Method ID 4 request. Best-effort cancellation of a dispatched future.
/// Cancellation is advisory: the inner call may have already completed.
/// Returns bebop.Empty on success. Returns NOT_FOUND if the future ID
/// is unknown.
public struct FutureCancelRequest: BebopRecord, BebopReflectable {
    /// The future to cancel.
    public let id: BebopUUID

    enum CodingKeys: String, CodingKey {
        case id
    }

    public init(id: BebopUUID) {
        self.id = id
    }

    public static func decode(from reader: inout BebopReader) throws -> FutureCancelRequest {
        // @@bebop_insertion_point(decode_start:FutureCancelRequest)
        let id = try reader.readUUID()
        // @@bebop_insertion_point(decode_end:FutureCancelRequest)
        return FutureCancelRequest(id: id)
    }

    public func encode(to writer: inout BebopWriter) {
        // @@bebop_insertion_point(encode_start:FutureCancelRequest)
        writer.writeUUID(id)
        // @@bebop_insertion_point(encode_end:FutureCancelRequest)
    }

    public var encodedSize: Int { 16 }

    public struct View: BebopRecordView {
        public let encoded: BebopView

        private let _decodeLimits: BebopDecodeLimits

        /// The future to cancel.
        public let id: BebopUUID

        public init(_ bytes: [UInt8], limits: BebopDecodeLimits = .default) throws { try self.init(BebopView(bytes), limits: limits) }

        public init(_ encoded: BebopView, limits: BebopDecodeLimits = .default) throws {
            var reader = BebopViewReader(encoded, limits: limits)
            self = try FutureCancelRequest.readView(from: &reader)
            try reader.finish()
        }

        fileprivate init(validated encoded: BebopView, limits: BebopDecodeLimits, id: BebopUUID) {
            self.encoded = encoded
            self._decodeLimits = limits
            self.id = id
        }

        public func decoded() throws -> FutureCancelRequest {
            try encoded.withUnsafeBytes { bytes in
                var reader = BebopReader(data: bytes, limits: _decodeLimits)
                return try FutureCancelRequest.decode(from: &reader)
            }
        }
    }

    public static func readView(from reader: inout BebopViewReader) throws -> View {
        let start = reader.position
        let id = try reader.readUUID()
        return View(validated: reader.view(from: start), limits: reader.limits, id: id)
    }

    public static let bebopReflection = BebopTypeReflection(
        name: "FutureCancelRequest",
        fqn: "bebop.FutureCancelRequest",
        kind: .struct,
        detail: .struct(
            StructReflection(fields: [
                BebopFieldReflection(
                    name: "id",
                    index: 0,
                    typeName: "BebopUUID"
                )
            ])
        )
    )

    // @@bebop_insertion_point(struct_scope:FutureCancelRequest)
}

// @@bebop_insertion_point(eof)
